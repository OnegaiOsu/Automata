services:
  - type: web
    name: automata-app
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
